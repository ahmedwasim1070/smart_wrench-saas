// Imports
import { Header } from "../components/Header";
import { LoginForm } from "../components/LoginForm";

//
const Login = () => {
  return (
    <section>
      <Header />
      <LoginForm />
    </section>
  );
};

export default Login;
